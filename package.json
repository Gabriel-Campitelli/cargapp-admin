{
  "name": "cargapp-admin",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "pm2 start ecosystem.config.js",
    "install:all": "npm install && cd packages/cargapp-admin-main-fe",
    "clean": "rm -rf node_modules && rm -rf packages/*/node_modules && rm -rf packages/*/dist"
  },
  "dependencies": {
    "glob": "^11.0.3",
    "pm2": "^6.0.13"
  },
  "devDependencies": {
    "dotenv-defaults": "^2.0.1"
  }
}
